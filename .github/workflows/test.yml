name: 🚀 Test

on:
  check_suite:
    branches:
      - main

jobs:
  run:
    name: 🚀 Test
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🚀 Test
        shell: bash
        run: |
          python3 -m unittest discover
