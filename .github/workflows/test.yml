name: 🚀 Test

on:
  pull_request:
    

jobs:
  run:
    name: 🚀 Test
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Test
        shell: bash
        run: |
          python3 -m unittest discover
